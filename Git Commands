Get Git URL : git remote get-url origin
git add: to add file into the staging area
git commit -m 'git message'
git push origin branch_name
git checkout -b branchname
git restore --staged filename or .
git log
git status
git rm filename
git touch newfile
git reset head~n
git rebase master
git pull
git fetch --all
git rebase -i head~n // for squash
git push -f origin branchname // for force push
